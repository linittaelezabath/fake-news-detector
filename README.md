# Fake News Detector (ML)

A simple Logistic Regression + TF-IDF model to classify news text as REAL or FAKE. Includes a training pipeline, CLI predictor, and FastAPI server.


## Setup
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

